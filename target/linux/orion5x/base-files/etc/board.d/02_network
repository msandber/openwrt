#!/bin/sh

. /lib/functions/uci-defaults.sh
. /lib/functions/system.sh

board_config_update

board=$(board_name)

case "$board" in
dlink,dns323)
	ucidef_set_interface_lan "eth0" "dhcp"
	;;
esac

board_config_flush

exit 0
